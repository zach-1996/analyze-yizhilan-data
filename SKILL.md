---
name: analyze-yizhilan-data
description: 通过已连接的一枝兰或九枝兰数据分析 MCP，查询并分析广告投放、招生转化、学校与渠道表现、素材、异常、定制报表、看板和收藏数据。用户提到“一枝兰”“九枝兰”，或明确调用 $analyze-yizhilan-data 时使用；招生人数、招生点数、报名量、信息、线索等近义业务词需要严格按语义映射，不能混淆。不要为不涉及一枝兰或九枝兰的普通数据分析自动触发。
---

# 一枝兰数据分析

## 目标

把用户的自然语言问题转成准确、可审计的只读 MCP 查询，再给出有数据证据的业务分析。覆盖单次查询、日报周报、同比环比、异常诊断、学校与渠道归因、转化漏斗、素材分析和定制报表。

## 必须遵守

- 只读当前已确认 MCP，不绕过 MCP，不读取或输出秘密。
- 指标、日期、渠道、媒体、院校归属和计算规则以 references 文档和实时 schema 为准；不固化 key、ID、报表编号或名称别名。
- 需要比较但未指定方式时默认公历同比；只有明确环比或指定上一相邻周期时才用环比；单值查询不强制增加对比期。
- 渠道、媒体和院校集合必须先确认实际对象与筛选能力；未返回对象不得当作 0，不能用名称包含关系替代地理归属或后台 ID。
- 任何省份、城市或区域查询都必须先执行“地域归属闸门”：将用户地域标准化为省份↔城市双向候选，完整发现后台院校后按地理归属和稳定院校 ID筛选；禁止仅按名称是否包含地域词、名称前缀或品牌名筛选。省份+品牌、城市+品牌及同区域不同品牌院校必须进入候选复核；无法确认的院校列为“待确认归属”。
- 指标字典无精确结果时才做受控兜底搜索；仍有多个不同口径候选时确认，不猜公式、不用近似指标替代。
- 区分事实、推断和建议；没有证据不写因果。只有歧义会改变结果时才提问。

## 工作流

### 1. 确认 MCP 绑定

首次运行或绑定失效时：

1. 查看当前会话暴露的 MCP 工具，按以下核心叶子名称识别候选，而不是假设服务器名称：
   - unify_table_master
   - company_metric_list
   - unify_spec_report_list
   - unify_spec_report_run
   - ai_insight_overview
   - material_dashboard
2. 若当前会话没有加载到候选 MCP，告知用户重启或重新打开会话以加载 MCP；不要直接访问其 URL。
3. 找到候选后，向用户展示可见的 MCP 显示名、工具命名空间和域名（若宿主提供），只问一次：“是否将此 MCP 作为一枝兰数据分析的数据源？”
4. 只有用户明确同意后，才用 scripts/mcp_binding.py confirm 写入本地绑定。
5. 后续先用 scripts/mcp_binding.py check 验证服务器名、命名空间和核心工具指纹。任何一项变化或核心工具缺失，都要重新展示候选并确认。

绑定文件位于用户本机的 Codex 状态目录，不属于共享技能。它只保存服务器名、工具命名空间、核心工具指纹和确认时间，不保存 URL、令牌、请求头或业务数据。

### 2. 解析问题

提取以下要素：

- 指标及限定词、日期及时区、对比方式、渠道范围、媒体/院校/事业部维度、筛选排序、输出范围和分析目的。
- 若涉及省份、城市或区域，先建立“地域候选清单”：用户地域、反向省份/城市别名、实际院校名称、稳定院校 ID、归属状态（纳入/排除/待确认）；在清单完成前不得开始院校×媒体下钻。
- 若用户说明后台采用“渠道不为线下渠道”后拉直接数据，记录该口径：直接指标 + 渠道维度拆分 + 排除“线下渠道”行，不使用网络限定字段替代。
- 没有日期的数值查询询问范围；仅为发现院校/媒体/指标等对象时可使用工具当前快照，并在口径中披露。
- 查询前读取 `references/business-semantics.md`；日期对比优先运行 `scripts/compare_dates.py`。

### 3. 获取当前字典并选择工具

- 每个独立分析重新读取当前指标/报表目录和工具 schema。
- 转化指标、有效信息、报名、招生点数和后端成本先查 `company_metric_list`；精确无结果才按业务语义做受控兜底，输出使用后台规范名称。
- 定制报表先列目录并解析当前 ID；标准查询优先 `unify_table_master`。
- 多院校/区域和媒体家族先做对象发现；事业部先确认是值筛选还是仅分组。
- 查询参数以实时 schema 为准，读取 `references/tool-routing.md` 后执行。

### 4. 执行最小且完整的查询

- 只传必要字段、维度、日期和筛选；渠道与定制报表按业务语义文档执行。
- 不涉及当日的周期默认截止前一天；使用报表式非线下口径时，拉渠道明细后先排除“线下渠道”行，再汇总直接指标并复核算术。
- 发现对象或拆分维度时完整分页；小页取数、按日期/维度/指标去重，未取完不得称为“全部”。
- 区域查询完成后执行覆盖复核：逐一检查完整院校发现结果中的省份命名、城市命名和同区域品牌院校是否已按映射处理，并在输出中列出纳入、排除和待确认集合；不得用“未出现在当前下钻结果”替代候选清单，也不得把未返回院校补为 0。
- 出现 `warning`、`truncated` 或 `truncated_json` 时按“减字段 → 减维度 → 拆日期/媒体/范围 → 分页”重试；`detail_count` 与明细冲突时视为计数元数据不可靠，继续翻页至空页并披露，不据此宣称总量完整；任何缺口都不补 0。
- 缓存成功页，失败只重试失败页；并发控制 3–4 个请求，默认不刷新缓存。
- 先取总览，再对异常对象下钻；检查参考日期、更新时间、权限和 0/空值/空结果/失败状态。

### 5. 校验和分析

涉及对比、异常、归因或周期报告时阅读 `references/analysis-and-reporting.md`。

- 校验日期、口径、单位、参考日期、权限、分页、重复聚合和总计/小计；自行计算只在公式和基础字段明确时进行。

### 6. 组织回答

默认顺序：

1. 周期与对比日期
2. 口径摘要：纳入/排除媒体、渠道范围、指标规范名称、汇总或重算方式、完整性与限制
3. 数据证据，包括必要的驱动因素或异常
4. 核心结论
5. 行动建议（适用时）
6. 指标口径、数据范围与限制

驱动因素与异常属于数据证据的一部分，放在结论之前。简单查询可省略行动建议，但仍按“周期 → 口径摘要 → 数据 → 结论 → 口径/限制”组织。

## 输出底线

- 默认表格只保留关键行和合计；不向用户倾倒原始 JSON。用户明确要求“各/全部”学校、渠道或其他对象时必须交付完整集合；结果过长则用分页或文件交付，正文只摘要关键行。
- 每次输出都固定先写“口径摘要”，至少包含：统计周期与对比周期、纳入/排除的媒体（使用后台规范名称）、渠道范围、指标名称（使用后台规范名称）、汇总或重算方式、结果是否完整及限制。
- 分平台结果仅保留后台媒体独立行；用户明确要求平台合计、全包含或家族合计时，才补充家族合计行，并明确合计范围。
- 排名只取了前 N 项时明确写“前 N”，不能写“全部”。
- 数量和金额默认展示本期、对比期和相对变化率；只有低基数、极端离群、绝对量很小却变化率很大、影响排名/结论、服务标记异常或用户明确要求时，再展示绝对变化。
- 比率指标默认展示百分点变化；相对增幅仅在有助于判断时补充。
- 每次输出都明确渠道口径：“网络口径（不含线下渠道）”“含线下渠道”或“分渠道（含全部渠道分类）”。
- 定制报表输出明确本次返回结果实际包含的院校；未出现在结果中的院校只标记为“本次结果未包含”，不得推断为报名量或其他指标为 0。
- 区域类输出必须说明地域映射口径，并列出实际纳入的院校；若用户使用城市名，默认同时检查该城市对应省份命名院校（例如“南京”需检查“江苏+品牌”），除非用户明确要求仅看名称带城市名的院校。
- 用户可见表格和结论不得出现 MCP 原始指标编号或内部标签；应显示当前字典确认后的业务名称，并在首次出现时说明口径。
- 行动建议必须能追溯到证据，并写清对象、指标、动作、预期方向和验证方式。

## 资源

- references/tool-routing.md：MCP 工具选择、目录预检、分页、缓存与降级。
- references/business-semantics.md：业务词映射、日期、对比、聚合、报表行语义和单位。
- references/analysis-and-reporting.md：分析深度、归因、异常、漏斗、输出模板与交付检查。
- scripts/mcp_binding.py：本地 MCP 绑定的状态、校验和确认。
- scripts/compare_dates.py：默认公历同比、显式环比、近 X 天和农历同比日期计算；相对周期的无后缀模式默认同比，只有 `previous-period`、日/周/月/季度环比及带明确环比语义的相对周期模式计算环比。
